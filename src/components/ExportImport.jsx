/** Placeholder — full export/import (JSON w/ base64 images, id-based merge) lands in fase 7. */
export default function ExportImport() {
  return (
    <div className="field">
      <label>Eksport / import</label>
      <p style={{ color: "var(--text-soft)", fontSize: 14 }}>Kommer snart.</p>
    </div>
  );
}
