// Proxy config — the Vinmonopolet API key lives in the Cloudflare Worker, never here.
export const PROXY_URL = import.meta.env.VITE_PROXY_URL;
export const APP_TOKEN = import.meta.env.VITE_APP_TOKEN;
